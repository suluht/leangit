Git is a version control system.
Gie is free sofeware.

git 语句的使用，
 git add 把文件加到仓库
git commit -m "文字说明"把文件提交到仓库
 commit可以提交多文件

git log 查看提交记录
git status 可以查看哪些文件被修改了
git diff 显示具体被修改了哪些内容

git reset -- head 和哈希函数生成的前几位就可以  回撤

git checkout --文件名   可以丢弃工作区的所有修改
